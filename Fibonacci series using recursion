
#include <stdio.h>

int fibb(int n)
{
    if (n==1 || n==2)
    {
        return n;
    }
    else 
    {
        return fibb(n-1)+fibb(n-2);
    }
}

int main()
{
    printf("%d",fibb(6));
    return 0;
}
